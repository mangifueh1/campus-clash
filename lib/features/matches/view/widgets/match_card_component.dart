import 'package:flutter/material.dart';

class MatchCardComponent extends StatelessWidget {
  const MatchCardComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card();
  }
}
